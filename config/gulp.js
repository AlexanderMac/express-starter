'use strict';

var config = {
  filters: {
    jsDeep: '**/*.js'
  },

  paths: {
    config: './config/',
    gulp: './tasks/',
    server: './server/',
    test: './test/'
  }
};

module.exports = config;
